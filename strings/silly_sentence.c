//andrea lugo, silly sentence c
#include <stdio.h>
//string variable for my use inputs (minimun of 3)

int main(void){
    //introduce user to program...tell them what is happening(print statement)
    
    //create uses input (print statements with question AND scanf to collect the info)(same as the number of variables)

//insert variable into the sentence to show the user (print statement, only 1) example: "hello %s", name

    return 0;
}